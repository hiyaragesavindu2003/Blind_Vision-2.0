# Optional: only needed if you want to switch the visual map back to
# Google Maps. The default uses Leaflet + OpenStreetMap (no key required).
VITE_GOOGLE_MAPS_KEY=
VITE_BACKEND_URL=http://localhost:3001
VITE_SOCKET_URL=http://localhost:3001
VITE_SUPPORTED_LANGUAGES=en,si,ta
VITE_WAKE_WORDS=assistant,guide,navigator
VITE_CONFIDENCE_THRESHOLD=0.75
VITE_EMERGENCY_CONTACT=
